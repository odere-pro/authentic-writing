# Vector Search Spike

We tried out pgvector for semantic search. We loaded about 100k embeddings and ran some queries. Latency was around 40ms at p50 and we played with a couple of index types. It was interesting and we learned a lot about how it behaves under load.
